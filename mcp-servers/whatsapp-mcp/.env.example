# WhatsApp MCP Server Configuration

# Session storage path (where browser session data is saved)
# Default: ./whatsapp_mcp_session
WHATSAPP_SESSION_PATH=./whatsapp_mcp_session

# Run in headless mode (no browser window)
# Set to "true" for production, "false" for development/debugging
# Default: false
WHATSAPP_HEADLESS=false
